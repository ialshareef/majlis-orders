# النشر على Cloudflare (قاعدة D1 + Worker واحد)

النتيجة: رابط واحد يفتحه كل الموظفين من أي جهاز، والطلبات مشتركة بينهم لحظياً. كل شيء في حساب Cloudflare واحد ومجاني.

يعمل **Worker** واحد كخادم للنظام: يخدم الواجهة (الصفحات والتصميم) ويدير الدخول والطلبات والأصناف والمستخدمين وسجل التحديثات على قاعدة **D1**. لا حاجة لـ Pages ولا لإعدادات CORS.

---

## الطريقة الأولى: نشر تلقائي بأمر واحد (موصى بها)

### 1) أنشئ رمز الوصول (مرة واحدة)
1. افتح <https://dash.cloudflare.com/profile/api-tokens>
2. **Create Token** ثم **Create Custom Token** (الخيار الأخير: Get started).
3. الاسم: `majlis-deploy`، وأضف الصلاحيات الثلاث التالية من قسم **Permissions**:

| النوع | الصلاحية | المستوى |
|---|---|---|
| Account | Workers Scripts | Edit |
| Account | D1 | Edit |
| Account | Account Settings | Read |

4. **Continue to summary** ثم **Create Token**، وانسخ الرمز الظاهر (يظهر مرة واحدة فقط).
5. احفظه في ملف باسم `.cf-token` داخل مجلد المشروع (بجانب `index.html`). أنشئه بالمفكرة والصق الرمز فيه واحفظه باسم `.cf-token` مع اختيار **All Files** في نوع الملف.

### 2) شغّل النشر

```bash
powershell -ExecutionPolicy Bypass -File deploy-cloudflare.ps1
```

السكربت ينفّذ كل شيء: ينشئ قاعدة `majlis-db`، وينشئ الجداول والبيانات الأولية، ويبني الـ Worker مع الواجهة بداخله، ويرفعه ويربطه بالقاعدة، ويفعّل الرابط العام، ثم يفحص التشغيل ويطبع الرابط النهائي:

```
==================================================================
  DEPLOYED
  URL   : https://majlis-api.<حسابك>.workers.dev
  Login : admin / admin   (change the password right away)
==================================================================
```

### 3) بعد النشر
- افتح الرابط وسجّل الدخول بـ `admin` / `admin`، ثم **غيّر كلمة المرور فوراً** من صفحة المستخدمين وأضف الموظفين.
- احذف ملف `.cf-token` من المجلد (لم يعد مطلوباً إلا عند إعادة النشر).
- لأي تحديث لاحق على النظام: شغّل نفس الأمر مرة أخرى، ولن تتأثر البيانات الموجودة.

---

## الطريقة الثانية: يدوياً من لوحة Cloudflare

إن فضّلت عدم استخدام رمز وصول:

1. **Storage & Databases > D1 > Create Database** باسم `majlis-db`، ثم تبويب **Console** والصق محتوى `worker/schema.sql` و**Execute**.
2. **Workers & Pages > Create > Create Worker** باسم `majlis-api` ثم **Deploy**.
3. شغّل `build-worker.ps1` محلياً، ثم **Edit code** في الـ Worker، احذف الكود الموجود والصق محتوى `worker/worker.bundle.js` كاملاً ثم **Deploy**.
4. **Settings > Bindings > Add > D1 database**: الاسم `DB` والقاعدة `majlis-db`، ثم **Deploy**.
5. افتح رابط الـ Worker من أعلى الصفحة.

> ملاحظة: `worker/worker.js` وحده (بدون بناء) يعمل كـ API فقط. الملف المبني `worker.bundle.js` هو الذي يحتوي الواجهة أيضاً.

---

## نقل بياناتك الحالية (اختياري)

إذا كانت لديك طلبات على الوضع المحلي:
1. في النسخة المحلية: ⚙️ الإعدادات > **تصدير نسخة (JSON)**.
2. في الرابط الجديد بعد الدخول كمدير: ⚙️ الإعدادات > **استيراد نسخة**. تُنقل الأصناف والطلبات والإعدادات والسجل. المستخدمون يُضافون من صفحة المستخدمين (لأن كلمات المرور تُخزَّن مشفّرة على الخادم).

## نطاق خاص (اختياري)

من صفحة الـ Worker: **Settings > Domains & Routes > Add > Custom domain**، وأدخل نطاقاً مثل `orders.example.com` (يجب أن يكون النطاق مضافاً في Cloudflare). مجاني وبشهادة HTTPS تلقائية.

## ملاحظات

- **الحماية**: لا تُقرأ أو تُعدَّل أي بيانات بدون جلسة دخول صحيحة. الموظف ينشئ الطلبات ويعدّلها فقط؛ أما إدارة الأصناف والإعدادات والمستخدمين وحذف الطلبات وسجل التحديثات فللمدير وحده (مطبَّق على الخادم لا في الواجهة). كلمات المرور محفوظة بتشفير PBKDF2 ولا يمكن استرجاعها.
- **الجلسات**: تبقى 30 يوماً على الجهاز. تغيير كلمة مرور موظف أو إيقافه يُنهي جلساته على الأجهزة الأخرى فوراً.
- **المزامنة**: كل 30 ثانية، وعند فتح صفحات الطلبات والسجل والأصناف والمستخدمين، وعند العودة للتبويب أو رجوع الإنترنت.
- **انقطاع الإنترنت**: يظهر تنبيه بفشل الحفظ وتبقى التعديلات في الصفحة لإعادة المحاولة. إذا عدّل موظفان نفس الطلب في آن واحد، آخر حفظ هو الذي يبقى.
- **الحدود المجانية**: 100 ألف طلب يومياً للـ Worker و5 جيجابايت لقاعدة D1، أكثر بكثير من حاجة محل.
- **النسخ الاحتياطي**: زر التصدير في الإعدادات يعمل هنا أيضاً، وD1 يوفّر استرجاعاً زمنياً (Time Travel) لآخر 30 يوماً من لوحة Cloudflare.
